/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    pageExtensions: ["tsx", "ts", "jsx", "js"], // Ensures Next.js detects your pages
};

module.exports = nextConfig;