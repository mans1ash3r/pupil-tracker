/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  pageExtensions: ["tsx", "ts", "jsx", "js"], // Ensure Next.js detects your pages
};

module.exports = nextConfig;